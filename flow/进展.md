# 进展日志（新的放最上面）

## 2026-09-23 · Windows(WSL2) 复现版打通 · WorkBuddy

- **背景**：手头只有 Windows 11 电脑，没有 Mac。原项目 Host 是 macOS 专属，目标是在
  Windows 上完整复现这套任务电台。
- **路线**：Host 跑在 **WSL2 Ubuntu + systemd**；管理界面用**命令行**替代 Tauri App
  （App 用 Unix socket 连 Host，Windows 原生进程进不了 WSL 内部通道，移植代价过大）。
- **链路打通**：真机验证完整闭环 —— 按住 S1 说话 → UDP 17333 → Qwen ASR → Codex 执行
  → 总结生成 → Qwen TTS → 按 S5 播放，第 5 颗灯与信箱灯行为正确。
- **补丁**：13 个文件、约 697 行，全部放在 `patches/`（Linux 适配 + 缺陷修复 + 固件修复），
  已用 `patch -p1 --dry-run` 验证可干净应用。
- **顺带修了 5 个原项目缺陷**：总结锁无回收、孤儿任务毒药循环、`contains("401")` 误判、
  日志无时间戳、固件录音队列溢出（详见 `踩坑记录.md`）。
- **产出**：`scripts/`（8 个部署脚本）、`tools/`（无板子模拟器 + 僵尸清理）、
  `docs/Windows复现指南.md`、`README.md`。
- **固件**：队列 64→256 帧 + 序号缺口容忍，已编译烧录；抓包确认烧后心跳恢复正常。
- **自检**：宿主测试 57/57 通过；补丁 dry-run 全清；真机跑通 4 条任务。
- **下一步（交接棒）**：① C4 验收表第 5 条（播放中 PTT 抢占）、第 7 条（旋钮断电重启档位）
  真机验证；② S3/S7/S8 补测；③ 可选：做「双模式固件」让语音与快捷键共存。

## 2026-08-10 · 本地 Wi-Fi 训练营分享版收口 · Codex

- 产品与真机：S1-S4 四路任务并发到红灯、S5-S8 四槽语音信箱、板载十档音量和短按音量播报均已
  完成现场验证；本次定义为本地 Wi-Fi 训练营分享版，长稳、断网和播放中 PTT 抢占继续留作压力测试。
- Host 与模型：总结固定为 `gpt-5.6-luna` / `high`，TTS 固定为北京区
  `qwen-audio-3.0-tts-flash` / `longanfengyue`；完成四路队列、Codex live catalog 有界重试、认证上行
  小缺帧容错和总结门禁简化，Host 只保留结构、归属、脱敏、缓存与有效音频等运行底线。
- 固件与 App：第五灯以绿/黄/橙/紫/红显示 0-4 个运行任务，左起四灯继续对应四槽未听信箱；旋钮
  控制板载扬声器并把档位保存到 NVS。桌面 App 已显示当前 DashScope 模型与 Host 状态。
- 课程材料：《Codex 任务电台：一把键盘，远程开发》已同步最新实现；PPTX/PDF 均为 36 页，SVG
  质量检查 0 错误、PPT 溢出检查 0 项，并完成 contact sheet 与关键页逐页目视复核。
- 验证：`RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，Host 259、CLI 6、parent-death 6、runtime
  gate 4、Desktop 5、firmware host 57，secret/source/license audit 全部通过。
- 发布：整批 App、Host、固件、文档和课程材料提交到 `main`，以 `v0.1.0-local-training` 标记训练营
  分享候选；仓库不包含 DashScope key、Wi-Fi 密码或本地运行数据。
- 下一步：按课程演示脚本做一次完整彩排，并把长稳、断网恢复与播放中 PTT 抢占作为后续迭代验收。

## 2026-08-10 · 删除无意义总结门禁并恢复第 3 槽信箱 · Codex

- 现场结论：本轮 S1-S4 均成功入队并完成，红灯已经由真机确认；S5/S6/S8 亮而 S7 不亮不是并发问题，
  而是第 3 槽 Luna 输出连续被事实字数、中文评分、证据摘录、旧数组逐项保留和新旧增量门禁拒绝。
- 简化：删除上述内容评分与二次重写门禁，也不再要求模型生成 `source_evidence`；只保留 JSON/字段尺寸、
  completion 归属、脱敏、缓存事务和有效 TTS 等运行底线。播报正文仍完整来自 Luna，不由 Host 拼接。
- 恢复：保留在 SQLite 的第 3 槽 completion 已由新 Host 自动重新领取，无需用户重说；Luna 生成后调用
  `qwen-audio-3.0-tts-flash`，已发布 generation 22、coverage 1，dashboard 的槽 3 已显示未读。
- 部署：Host 已升级到 PID 54509；构建产物与安装二进制 SHA-256 同为
  `1038ef0b7e8020844152107a488f67b2f55eaca1e9e619e6f79a4bec6cf1fab8`。
- 验证：Host lib 259 项全绿，包含 Spark prompt、总结事务、TTS/cache、四路队列和上行缺帧测试；
  `git diff --check` 通过。软件状态证明槽 3 已发布，但仍等待用户目视确认 S7 对应信箱灯已点亮。
- 下一步：用户确认 S7 灯后，更新 HIL 勾选并提交、推送当前整批 App/Host/固件/PPT 改动。

## 2026-08-10 · App 当前模型恢复，四槽并发提升到 4 · Codex

- App/Host：确认 live dashboard 一直返回 DashScope 北京区 `configured=true`；旧安装进程与失败后残留字段
  让界面看起来仍是旧模型。现已重新构建并安装 `/Applications/Codex Keyboard.app`，失败时会清空旧值并
  区分“Host 不可达”和“Host 响应异常”；当前界面实证显示 `qwen3-asr-flash`、
  `qwen-audio-3.0-tts-flash`、`longanfengyue`，音色不再被截断。
- 并发：Host 的全局 prompt 上限由 2 提升为 4，四个不同 task 可同时运行；同一 task 仍严格 FIFO。
  因此键盘自身现在能触发第五灯的黄/橙/紫/红四个忙碌等级，而不再最多停在橙色。
- 部署：release Host 已升级到 PID 76965，安装二进制与构建产物 SHA-256 同为
  `70c3c229bce2c2b63514c96909df1e00c5d7bc2e4ef218bf6578822e31a7dde9`；App 两份二进制同为
  `b4efecaf80a7eefe9ba68a6c5b22991c19f79eafc0493aab9f7acb61887092ce`。
- 验证：顺序 `RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，Host 271、CLI 6、parent-death 6、
  runtime gate 4、Desktop 5、firmware host 57，secret/source/license audit 均通过；四路 scheduler 和
  store 上限定向测试再次通过，live dashboard 为 18 个任务、四槽绑定可读。
- 课程材料：第 15 页换成当前真实 App 截图，第 18 页讲稿补“四个不同任务可并行、同一任务仍排队”；
  PPT Master 重新导出 36 页 PPTX，浏览器重新生成 36 页 PDF 与 contact sheet，PPT notesSlide18 已读回。
- 下一步：本轮代码和部署已完成，但紫/红仍需实体键盘 HIL。连续向 S1-S4 提交四个至少运行 60 秒的
  不同任务，确认第五灯黄→橙→紫→红以及全部完成回绿后，再提交并推送这一整批改动。

## 2026-08-10 · 旋钮改为板载十档音量并加入短按播报 · Codex

- 产品修正：旋钮不再向 Mac 发送系统音量键；V2 物理逆时针降低键盘扬声器音量、顺时针提高，
  共 10 档，默认 70%。增益在 I2S PCM 写入前逐帧读取，因此正在播放和后续播放都会立即采用新档位。
- 设备闭环：停止旋转 1 秒后将档位写入 NVS `spk_vol_v1`，重启继续使用；短按旋钮播放固件内
  对应的“当前音量百分之 X”，长按 3 秒配置模式与 S1-S8 保持不变。即使本次开机跳过开机音，
  短按也会自行拉起扬声器，不依赖 Mac 或网络。
- 语音资产：用北京区 `qwen-audio-3.0-tts-flash` / `longanfengyue` 预生成十条 48 kHz 中文提示，
  转换为 EIAD v1 并嵌入 App 镜像；manifest 记录每档样本数、编码字节数和 SHA-256，源码与仓库无 key。
- Host 修复：兼容 Qwen Audio 3.0 实际返回的 seekless WAV 长度哨兵，并拒绝不完整哨兵组合；release
  Host 已升级到 PID 35655，health `ready`。升级后 live generation 176 已从 `tts_start` 进入
  `summary_worker=published`，传输 183346 bytes / 357700 samples，并收到设备完成协议，确认合法 WAV
  不再被误判为 `tts_ambiguous`；是否真实听清仍不以协议日志替代人耳验收。
- 验证：`RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，包括 Host 271、CLI 6、parent-death 6、
  runtime gate 4、Desktop 4、firmware host 57，以及 secret/source/license audit；ESP-IDF v5.5.5
  esp32s3 production build 成功，镜像 0x20f2b0，3 MB App 分区剩余 31%。
- PPT：第 10/12/31 页和讲稿改为板载十档音量与短按播报；SVG 36/36 无错误，PPTX/PDF 都为
  36 页，三张改动页逐页渲染无截断。候选 SHA：bootloader
  `4f53dc9147728131b891a5a8a2524ad2bb60c43cb573db8827b29b2bdba0d314`；partition
  `7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`；app
  `b54b16e60dc46f5d72163c32bdac59d44c331f0d176e9017bc4a025e1fb99578`。
- 下一步：本轮尚未烧录、未提交推送。获得用户针对以上三个精确 SHA 的授权后，按 USB
  debug-reset 等待法烧录；真机验收旋转方向、短按十档播报、播放中即时调节和断电重启保持。

## 2026-08-10 · 已废弃：曾误解为 Mac 系统音量的候选（未烧录） · Codex

- 错误理解：当时把旋钮误做成系统音量控制，V2 物理逆时针映射 HID Consumer Control Volume
  Decrement，顺时针映射 Volume Increment；旋钮按压和 S1-S8 行为不变。
- 传输：USB 与 BLE 都新增标准 16-bit Consumer Control report；每个物理步进以按下/释放成对入队，
  端点忙时保留，USB epoch 或 BLE owner generation 改变后不向新主机重放旧步进。BLE GATT schema
  revision 升到 5，使已配对主机获知 report map 变化。
- 自动验证：`RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，Host 269、CLI 6、parent-death 6、
  runtime gate 4、Desktop 4、firmware host 57，secret/source/license audit 均通过；ESP-IDF v5.5.5
  production build 为 esp32s3、16 MB Flash、Octal PSRAM。
- PPT：第 10 页补旋钮交互，第 12 页改为“调音量”，第 31 页补旋钮旋转/按压和自定义固件能力；
  PPT Master SVG 36/36 无错误，PPTX 含 36 页和 36 页备注，浏览器 PDF 36 页，三张改动页逐页检查通过。
- 候选镜像：bootloader SHA-256 `4f53dc9147728131b891a5a8a2524ad2bb60c43cb573db8827b29b2bdba0d314`；
  partition `7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`；app
  `6d4f88a0453edf064c207e6ce527614a732066aa9b9cc413ddca44feb82ca1ab`。
- 处置：该候选从未烧录、从未提交推送，三个旧 SHA 已作废；后续板载音量实现完整删除了这条错误路径。

## 2026-08-10 · 总结切换 Luna High，整段音频切换 Qwen Audio 3.0 · Codex

- 总结：生产模型改为 `gpt-5.6-luna`，reasoning effort 固定 `high`；prompt 删除 120~180 字目标，
  改为按信息量决定长度，短确认不扩写，普通结果只保留核心结论、数字、限制、决定和必要行动。
- TTS：生产链改为北京区 `qwen-audio-3.0-tts-flash` / `longanfengyue`；Host 把完整
  `spoken_text` 只 POST 一次，不再按 40 字切块。服务返回的北京区 OSS 地址只允许精确白名单，
  即使返回 `http` 也强制用 HTTPS 下载；完整 48 kHz mono PCM16 WAV 校验后才进入原有加密缓存。
- 兼容与失败语义：旧 `qwen3-tts-instruct-flash-realtime` 未听缓存仍可读取；新 TTS 请求一旦可能
  已提交就不自动重发，避免重复合成和计费，失败仍保留 completion 和旧未听 generation。
- 验证与部署：真实 DashScope 冒烟返回 48 kHz mono PCM16、7.132 秒完整 WAV；顺序
  `RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，Host 269、CLI 6、parent-death 6、runtime gate 4、
  Desktop 4、firmware host 57，secret/source/license audit 均通过。release Host 已升级到 PID 39942，
  health `ready`，live dashboard 显示新 TTS 模型、音色和 `configured=true`。
- 课程材料：第 22、23、34 页及讲稿同步 Luna High、Qwen Audio 3.0 和整段一次生成；PPT Master
  36/36 导出成功、SVG 0 error，PPTX 含 36 页与 36 页备注，浏览器 PDF 36 页和 contact sheet 已更新。
- 下一步：从下一条新完成任务开始使用新链路；第一次键盘真机播放仍需主观确认总结是否更像简洁工作汇报、
  龙安风玥音色是否更自然。旧缓存不会被重写。

## 2026-08-10 · 第五灯候选完成烧录并审视四槽摘要质量 · Codex

- 烧录：用户针对三份精确镜像授权后，按 USB debug-reset SOP 只写入 `0x0` bootloader
  `4f53dc9147728131b891a5a8a2524ad2bb60c43cb573db8827b29b2bdba0d314`、`0x8000` partition
  `7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278` 和 `0x10000` app
  `96285d918fa8eb8166d36bca3a9c6f54d6b14b3048e9bab16808ebf2a3624652`；三段均通过 hash 校验，
  未擦 NVS、Wi-Fi 或音效分区，复位后重新枚举为 `EasyInput AI`。
- Host/live：release Host 升级到 PID 6065，health `ready`；观测到 1280 bytes 入站和 512 bytes
  出站，等于 16 轮认证心跳及 `EIMB v3` 回包。用户确认当前灯光可用；本轮只确认空闲绿色，
  黄/橙/紫/红和全部完成回绿仍是后续并发任务 HIL。
- 摘要取证：最近四条已经完整播放并按合同删除缓存，无法也不应从已删缓存伪造原 `spoken_text`；
  从四个绑定 Codex 任务读取了当时真正进入总结的助手最终回复，并从 Host 日志核对最后播报长度为
  S1/S2/S3/S4 = 64/97/179/220 字。
- 质量判断：当前 `gpt-5.3-codex-spark` 在真实 S3/S4 价格类回复上即使被要求压缩，仍倾向完整复述；
  `gpt-5.6-terra` 用同一批输入约 10 秒完成，能保留核心价格、分辨率和限制并明显缩短。生产 prompt 的
  120~180 字目标也会把短确认扩写，因此建议同时改为 Terra 和按信息量自适应长度；Qwen TTS 与 Cherry
  暂不调整。本轮只完成审视，没有改动 live 总结模型或生成新 TTS。

## 2026-08-10 · 第五灯改为 Codex 任务活动灯并更新课程 PPT · Codex

- 产品语义：左起第 1~4 颗仍只表示槽 1~4 的未听语音信箱；最右第 5 颗只表示不同已绑定任务的
  运行数，0/1/2/3/4 个依次为绿/黄/橙/紫/红，最后一个任务完成后回绿。
- Host/协议：rollout observer 按权威 `task_started -> task_complete` 统计运行 task，重复绑定只计一次；
  认证状态包升级为固定 32-byte `EIMB v3`，新增 0~4 运行数并保留四槽 coverage 与 HMAC。
- 固件：`0.6.0-easy-codex-task-activity` 将物理最右 D1/frame 0 映射为任务活动灯，左起
  D5~D2/frame 4~1 继续映射槽 1~4；短时按键/录音/播放反馈结束后恢复五灯产品状态。
- PPT：更新第 10、18、20、34 页及讲稿，明确“前四颗看信箱，第五颗看忙不忙”；PPT Master
  36/36 导出成功、SVG 0 error、PPT overflow 0、浏览器 36 页渲染和 PDF 36 页通过。
- 自动验证：首次并行 fast eval 仅 `daemon_started_before_first_provision_reloads_device_secret` 超时，
  单测立即通过；随后 `RUST_TEST_THREADS=1 ./scripts/eval-fast.sh` 全绿，Host 267、CLI 6、
  parent-death 6、runtime gate 4、Desktop 4、firmware host 57，secret/source/license audit 均通过。
- 构建：ESP-IDF v5.5.5、esp32s3、16 MB production build 成功。bootloader SHA-256
  `4f53dc9147728131b891a5a8a2524ad2bb60c43cb573db8827b29b2bdba0d314`；partition
  `7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`；app
  `96285d918fa8eb8166d36bca3a9c6f54d6b14b3048e9bab16808ebf2a3624652`。
- 部署边界：本轮没有升级 live Host、没有复位或烧录键盘，避免旧 EIMB v2 固件与新 Host 暂时失配；
  下一步是用户针对以上三个精确 SHA 授权后按既定 USB debug-reset SOP 烧录，再升级 Host 并做五色 HIL。

## 2026-08-10 · 完成《Codex 任务电台》36 页课程案例 PPT · Codex

- 内容：完成 30 页主讲与 6 页技术附录，从真实工作麻烦讲到八键对讲机想法，再用人话解释
  四槽绑定、语音输入、Spark 总结、Qwen TTS、本地缓存、灯光信箱和真机验收；明确“远程”当前只指
  离开 Codex 窗口，同局域网产品并未宣称公网遥控。
- 视觉：使用 EasyInput V2 实物、PCB、键位图和脱敏 Mac App 截图；另生成 4 张只承担概念表达的
  无文字插图，不把生成图当作真实产品证据。全套采用雾白、石墨、信号绿、频道蓝的轻工业教学风格。
- 讲稿：36 页均写入自然中文演讲备注，保留为什么想到这个 idea、实际踩坑和判断“真的好了”的证据层，
  不使用内心独白，也避免“赋能、范式、闭环”等套话。
- 验证：SVG 质量检查 36/36 无错误、spec lock 无漂移；PPT Master 导出 36/36 成功并带 36 页备注；
  最终 PPTX ZIP 结构无错误，浏览器逐页渲染 36/36 并生成 contact sheet，PDF 为 36 页。
- 产物：`projects/codex-radio_ppt169_20260810/exports/Codex任务电台_课程案例_Lark.pptx`、
  `projects/codex-radio_ppt169_20260810/exports/Codex任务电台_课程案例_Lark.pdf`；下一步只需现场
  打开 PowerPoint 检查讲者电脑字体与投屏比例，再按真机演示节奏排练。

## 2026-08-10 · 优化 Spark 总结顺序与 Qwen TTS 听感 · Codex

- 总结内容：只调整 Spark prompt，不向 `spoken_text` 塞入固定正文。新播报立即以最后一条
  completion 的最新具体结果开头，再压缩融合仍相关的旧未听结果/决定；只在来源确有
  可执行待办时才以下一步收尾，不再加“可以继续”类空泛结尾。
- 播报长度：普通 1~5 条 completion 以约 120~180 个中文字符为听感目标，但有重要旧未听
  内容时允许超出，不为了追求短而丢掉有用信息；原 480 语义字符/150 秒安全边界未变。
- TTS 听感：保留 `Cherry` 和北京区 `qwen3-tts-instruct-flash-realtime`，指令改为自然同事式女声、
  中等偏快、句间短停顿，弱化播音腔/客服腔，并让英文技术词、数字和缩写按自然语境朗读。
- 验证与部署：Host 266/266 和 `./scripts/eval-fast.sh` 全绿，包括 CLI 6、parent-death 6、
  runtime gate 4、Desktop 4、firmware host 57、secret scan 和 source/license audit；release Host
  已升级为 PID 71113，health `ready`。
- 边界：已缓存的旧音频不会被重写；从下一条新完成任务开始使用新 prompt 和新音色指令，
  真实“更自然/更快”听感以下一条键盘播放为物理验收点。

## 2026-08-10 · 四槽语音信箱真机闭环完成 · Codex

- 真机验收：S1~S4 分别完成按住语音输入、ASR 和对应 Codex 任务入队；
  左起第 1~4 颗灯分别表示四槽未听数量，第 5 颗保持熄灯；S5~S8 分别播放对应槽位的
  Spark `spoken_text`，用户已确认四槽全部可用。
- 收敛证据：S3 generation 4 / coverage 4 与 S4 generation 4 / coverage 5 都完成
  `begin_ack -> transfer_complete -> device_finished -> heard`；四槽 `unread_generation` 均为 null，
  已听缓存删除且对应灯熄灯。
- 根因修复：旧 TTS `Ambiguous` 且无认证缓存时不再永久占用槽位；Host 原子释放
  claim 但保留 completion，下一轮可直接重新总结和合成，S3/S4 堵塞已现场解除。
- 自动验证：`./scripts/eval-fast.sh` 全绿，Host 265、CLI 6、parent-death 6、runtime gate 4、
  Desktop 4 和 firmware host 57 项测试全部通过；格式、Clippy、桌面构建、secret scan 与
  source/license audit 均通过。
- 下一步：不再增加摘要内容硬编码；根据真实听感小幅调整 Spark prompt 的“最新结果优先”和
  Qwen TTS 的自然女声、语速与停顿，保持未听内容累计语义。

## 2026-08-10 · 修复 S3/S4 摘要被旧 TTS 模糊状态永久阻塞 · Codex

- 现场根因：S3/S4 的 ASR 和 Codex 入队都成功，但两个任务各保留一条旧 generation 3 TTS
  `Ambiguous` 尝试。旧逻辑为避免重复调用 TTS，一直保留 `generating` 并要求人工对账，后续 completion
  因此只能持续得到 `another summary generation is already running`，不会产生缓存或亮灯。
- 修复：`SummaryOrchestrator` 现在先尝试恢复已认证缓存；如果根本没有可播放的认证产物，就原子
  abandon 旧 TTS 尝试、保留原 completion，并立即允许新 generation 重试。同样处理 TTS 模糊回执、
  错音色和无认证缓存的 commit 失败，不再把槽位永久占住。
- 自动验证：Host 265/265 测试通过，包含“已持久化 Ambiguous 在重启后释放”、“TTS 模糊回执无需
  重启即释放”、“缓存 commit 失败释放”和“错音色释放”回归；release Host 已升级到 PID 40959。
- 运行验证：两条旧 generation 3 均自动 abandon，随后 S3 发布 generation 4 / coverage 4，S4 发布
  generation 4 / coverage 5；dashboard 已同时标记两槽未读。
- 下一步：用户确认物理第 3/4 灯已亮，再按 S7/S8 分别播放至完成；Host 验证两槽精确
  `device_finished -> heard`、缓存删除且对应灯熄灭，之后再提交本批。

## 2026-08-10 · 用户经 EasyInput 恢复键盘 LAN 配置 · Codex

- 做了什么：用户直接用 EasyInput 重新写入键盘配置；Codex 不再访问钥匙或改写配置，只通过
  BLE 状态和 Host 进程网络计数做只读验证。
- 验证：键盘回报新配置 `saved=true` / 1123 bytes / CRC16 3878；Host PID 29801 每 2 秒稳定收到
  80 bytes 心跳并回复 32 bytes，连续四轮一致，说明键盘到 `192.168.1.70:17333` 的 LAN 控制链已恢复。
- 信箱状态：Host dashboard 仍保留 S1 generation 12 / coverage 1，S2-S4 无未读；本轮未重启 Host、
  未消费音频。
- 下一步：先由用户确认物理左一灯已恢复，再按 S5 播放 S1 的旧未听；Host 同步追踪
  `begin_ack -> transfer_complete -> device_finished -> heard`，最后确认缓存删除和左一灯熄灭。

## 2026-08-10 · 通过 BLE 读取重启后的键盘真实状态 · Codex

- 做了什么：不改键盘配置，通过已连接的管理 BLE 特征直接读取固件状态；确认运行固件为
  `0.5.0-easy-codex-lan-playback`，保存配置 `saved=true` / 1124 bytes / CRC16 15215，音频能力已启用且麦克风
  `mic_ready`。
- 根因：键盘 NVS 中的 Host 是 `192.168.1.71:17333`，而 Mac 当前是 `192.168.1.70`；重启后固件
  状态停在 `wifi_preparing`，Host 收不到心跳。Wi-Fi 密码未被判定为变更，真正需要更新的只是
  Host 地址。
- 安全边界：现有 `provision-lan` 是整份原子配置，为了保留原密码必须原样带回；尝试从
  System Keychain 读取时发现 Mac 已锁屏，因此立即中止等待。本轮未读出/打印密码、未改 `.env`、
  未下发配置、未重启 Host，S1 未听仍保留。
- 下一步：用户解锁 Mac 后，一次性读取原密码并更新 Host 为 `192.168.1.70`，写入 0600 本机
  `.env` 并经 USB HID 收到精确保存 ACK；随后验证键盘心跳、左一灯恢复和 S5 听完后灯熄灭。

## 2026-08-09 · 板子重启后信箱灯不恢复的现场定位 · Codex

- 做了什么：通过 Host 唯一的 dashboard IPC 确认 S1 的 generation 12 仍是未听，缓存没有丢；
  键盘 USB HID 也仍正常枚举。同时确认 Host PID 29801 正在 UDP 17333 监听，但连续 5 秒
  进程网络采样为 0 bytes，说明重启后键盘没有向当前 Mac 发心跳，灯不亮不是信箱状态被清除。
- 怎么理解：板子 `192.168.1.15` 可 ping，Mac 当前是 `192.168.1.70`；故障点收窄到板子 NVS 中
  保存的 Host 端点/鉴权配置没有收敛到当前 Host，需要通过 USB HID 重新下发完整 LAN 配置。
- 问题→解决：macOS 钥匙读取连续等待授权但未获得结果，已中止等待进程，没有打印密码、
  没有下发空密码，也没有重启 Host 或消费 S1 未听。
- 下一步：用户完成一次“始终允许”钥匙授权后，立即把 SSID/密码/Host/Port 保存到
  `~/Library/Application Support/EasyCodexInput/.env`（0600）并 USB HID 下发；然后先验证左一灯恢复，
  再按 S5 完整追踪 `device_finished -> heard -> 灯熄灭`，期间不重启 Host。

## 2026-08-08 · 摘要收窄为助手最终回复并优化预下载等待 · Codex

- 做了什么：确认刚才 S1/S2 实际绑定的是测试任务 `hi`/`hi-2`，不是当前 `easy-codex-input`；旧实现会把
  turn 的用户消息、工具事实和多条过程回复一起交给 Spark。现在以权威 `task_complete.last_agent_message`
  覆盖中间助手消息，并在 Spark prompt 构造时只序列化每个 completion 的最后助手回复，旧 turn pack
  也按同一规则读取；未听旧摘要仍按原有精确累计合同保留。
- 延迟原因与优化：TTS 已预生成并加密缓存于 Mac，但固件在 PSRAM 收完整段 EIAD 后才启动扬声器；本次
  S1 为 67 秒、1.66 MB，因此首声前包含完整 LAN 传输。Host 有界发送窗口由 4 包提升到 16 包，固件单轮
  接收由 8 包提升到 16 包，并新增无正文的 `elapsed_ms` 传输计时；真正边下边播仍归 L2B。
- 验证与部署：`eval-fast` 全绿（Host 259、CLI 6、parent-death 6、runtime gate 4、Desktop 4、firmware
  host 57），secret/source/license audit 全绿；ESP-IDF v5.5.5 production build 成功，Mac LaunchAgent 已
  升级且 health ready。听完的 S1/S2 已收到 exact `device_finished -> heard`，缓存删除且两槽无未读。
- 当前边界与下一步：新固件 app SHA 为
  `b66d886f09ef83c9135481e772bf82e1ff686040994344411eac55ac534e2c69`（bootloader/partition 未变）；
  等用户对三个精确 SHA 授权后按 `usb_reset` SOP 烧录，再用新生成的助手最终回复摘要测量首声等待与内容可懂度。

## 2026-08-08 · 固化 USB debug reset 烧录法并写入 L2A 固件 · Codex

- 做了什么：把 V2 板的强制烧录 SOP 写入根 `AGENTS.md`；烧录方先运行端口等待与
  `esptool --before usb_reset --connect-attempts 0`，用户只需断电再上电，不再默认要求按 BOOT，
  并明确禁止已两次导致 `Device not configured` 的 `--before no_reset` 做法。
- 固件与验证：按用户对精确 SHA 的授权，仅写入 `0x0` bootloader
  `4f53dc9147728131b891a5a8a2524ad2bb60c43cb573db8827b29b2bdba0d314`、`0x8000` partition
  `7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`、`0x10000` app
  `62c8488db4876f2cbfaabbadc0d44343dceeda05cddc3ef44c1b42b55203ed1f`；三段均完成
  `Hash of data verified`，未擦 NVS/声音数据，复位后已重新枚举为 `EasyInput AI`。
- 自动化：本批完整 eval 全绿，Host 259 项、firmware host 57 项及 secret/source/license audit 均通过。
- 当前边界与下一步：S1 音频已完成 Host 到设备的传输，未再出现重复按键导致的 `status=3`；仍需用户
  确认真机完整可听，并等待匹配的 `device_finished -> heard -> cache delete -> 第一槽灯熄灭`，之后再单独测 S6。

## 2026-08-08 · L1 同 Wi-Fi 中文语音闭环真机可听 · Codex

- 做了什么：完成四槽 Desktop/Host IPC、DashScope 北京区状态、S2 PTT/ASR、Codex task FIFO、Spark 中文累计摘要、Qwen realtime TTS 加密缓存和 S6 键盘扬声器回放；用户已确认真机有声音。
- 固件与运行时：最终烧录 `bootloader=6ba36341b4f47b1e10d6d6017c12fbdb102fa66eccec8ce42841c99e803e2d07`、`partition=7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`、`app=b06934cfc8f3636bac779bda24485c87afe592734703c5fbd04c7d317ab18873`；保留 NVS。Host live health 正常，Desktop 未签名 macOS bundle 构建成功。
- 问题与解决：修正长摘要被开机音上限拒绝、Host/固件 EIAD 布局不一致、失败后 Host 长等、单分片停等过慢、TTS ambiguous 恢复卡住和英文摘要；播放改为四分片窗口，重复 gap ACK 有界重试，纯英文在 TTS 前仅允许一次有界再生成，否则保留 completion 且不合成。
- 验证：S6 实机完成 begin/ack、270686-byte 传输、device finished、exact heard 和缓存文件删除；`eval-fast` 全绿（Host 248、CLI 6、parent-death 6、runtime gate 4、Desktop 4、firmware host 56），secret/source/license audit 全绿。Sol high 首审 `P0=0/P1=0`，两个 P2 修复后复审关闭。
- 当前边界与下一步：本批代码可提交推送；L1 仍保留最后一个真机门禁，需生成新未读并在 S6 播放时按 S2，确认 PTT 立即抢占且未读不被误消费，之后再进入 ring streaming 和长稳测试。

## 2026-08-08 · L1 固件候选已构建，等待精确 SHA 烧录授权 · Codex

- 做了什么：在新仓用 ESP-IDF v5.5.5 从 `db738c9` 构建 `esp32s3` / 16 MB Flash / 8 MB Octal
  PSRAM 候选；app 大小 `0x18eb40`。新增 L1 任务卡，把四槽 Desktop 绑定、DashScope 状态和同 Wi-Fi
  真机闭环纳入同一阶段。
- 候选：`0x0 bootloader=434b1831f7cb0ab04fc5402022bf90f6ab738ce69be78927e5ac5d3d8c52ea3d`；
  `0x8000 partition=7c541b70dcac8f920c2d11589f06745e1b033fa9b95b8343de2748bb8312a278`；
  `0x10000 app=50e402e271b4a60ea53610bf49a35315f705f864799b38505b2ddc8574813663`。
- 当前边界：尚未复位或写入实体板；板子处于 TinyUSB HID 状态，没有稳定串口属于预期。Host live health、
  Codex catalog 和 DashScope 私有 `.env` 均已确认 ready，未读取或输出 key。
- 下一步：用户确认上述精确 SHA 后，先启动 ROM 端口轮询，再执行“USB 保持连接、关电源、按住 BOOT、
  开电源并继续按住”抓上电窗口，只写三个固件地址且不擦 NVS；烧录后继续四槽 App 和 HIL。

## 2026-08-08 · 从多网络工程抽取 Codex Keyboard 本地 Wi-Fi 仓 · Codex

- 做了什么：以 `easy-codex-input@52949d33c51bac605a33cb8ff42ee3eeab37021e` 为冻结来源，建立
  `Codex_Keyboard` 独立目录并连接公开仓库 `Larkspur-Wang/Codex_Keyboard`；原仓保持 clean 且 HEAD
  不变。新树删除 PWA、Cloudflare Relay、远程 envelope 和相关证据/计划，只保留 Mac Host、Tauri、
  ESP-IDF 固件、USB/BLE 配网、LAN PTT、自动总结、加密缓存和 LAN 扬声器播放。
- 为什么：当前优先目标是尽快把同 Wi-Fi 真键盘闭环跑通，不再让远程网络、手机代理和浏览器生命周期
  扩大调试面。公开新仓使用无旧历史的根提交，但在 README/任务卡保留精确来源 commit 和许可证边界。
- 验证：`eval-fast` 全绿（Host 238、CLI 6、parent-death 6、runtime gate 4、Desktop 2、固件 56），
  secret/source audit 全绿。Sol high 首轮发现旧历史和 `source` push 跟踪两个 P1；已改为单一无父根提交、
  `main -> origin/main`，并禁用 `source` push，复审为 `P0=0/P1=0`。
- 当前边界：新仓 `main` 已推送到公开仓；实体键盘保持正常 HID，本轮不复位、不烧录、不消费保留的
  未读音频。误建的空私有仓 `easy-codex-input-local-wifi` 已归档，当前 token 无删除权限。
- 下一步：在新仓锁定 ESP-IDF v5.5.5 重新构建候选镜像，记录精确 SHA，请求烧录授权后进行 S6
  同 Wi-Fi 可听播放 HIL。
