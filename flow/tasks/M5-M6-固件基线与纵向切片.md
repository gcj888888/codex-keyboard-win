# M5-M6 固件基线与纵向切片

## 状态

- 阶段：基线与共享核心技术关闭；公开推送等待权利授权；真传输/HIL 继续进行
- 启动：2026-08-04
- 来源：`easy-input-keyboard@7a41ce1c5bd961f3be417498cee1a8131ab9d86e`

## 用户决定

不再把固件迁移留到模拟器之后。先把已经真机验证的完整 V2 ESP-IDF 基线复制到本仓 `firmware/`，随后
由真固件和 simulator 共用四槽/PTT/playback 状态机。来源仓只读，本仓成为 Easy Codex Input 固件的后续
真相源。

## 本阶段交付

1. 复制 V2 `components/`、`main/`、`features/`、`diagnostics/`、`host_test/`、产品 CMake、sdkconfig 和
   partitions；保留本仓已有 `eci_protocol` 与三语言 golden vectors。
2. 记录精确来源 commit、复制清单、上游 Apache-2.0 `esp_hid` 许可证与根仓尚无显式许可证的边界。
3. 合并 Host tests，完成全部 V2 测试、`eci_protocol_test` 和锁定 ESP-IDF v5.5.5 产品构建。
4. 新建共享四槽设备状态机，再接 simulator 的断线、乱序、ACK 丢失、播放抢占与 generation 测试；
   共享核心以 1,000 个 seed、每 seed 100 次操作验证设备侧不跨槽、不接受旧完成。
5. 软件、模拟器和真板结果分开报告；未经用户针对候选镜像明确授权，不复位、进下载模式或烧录实体键盘。

## 退出条件

- 完整 V2 基线已经在本仓独立构建，来源仓没有被修改。
- 1,000 个 seeded trace 无跨槽、错误 finished；错误删除和重复 Codex turn 由完整 Host 纵向切片证明，
  不把没有持久化/删除职责的固件 RAM 状态机当作越权证据。
- Sol high 独立审查、提交推送和 exact-commit CI 全绿。
- 真板烧录与扬声器/PTT HIL 仍属于单独用户授权门禁。
